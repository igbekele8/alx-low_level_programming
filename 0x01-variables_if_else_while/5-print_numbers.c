#include <stdio.h>

/**
 * main - A program that prints all single digit num of base 10 start from 0
 *
 * Return: always 0
 */

int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
